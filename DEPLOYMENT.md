# 部署指南

## 📦 Netlify 部署完整步骤

### 前置准备

1. 确保已有 GitHub/GitLab/Bitbucket 账号
2. 注册 [Netlify](https://www.netlify.com/) 账号（可以用 GitHub 账号直接登录）

### 步骤 1: 推送代码到 Git 仓库

```bash
# 初始化 Git 仓库（如果还没有）
cd website
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: 小习惯 App 官方网站"

# 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/your-username/xiaoguanxi-website.git

# 推送到 GitHub
git push -u origin main
```

### 步骤 2: 在 Netlify 创建站点

1. 登录 Netlify: https://app.netlify.com/
2. 点击 **"Add new site"** → **"Import an existing project"**
3. 选择 **"Deploy with GitHub"**（或其他 Git 提供商）
4. 授权 Netlify 访问你的仓库
5. 选择 `xiaoguanxi-website` 仓库
6. Netlify 会自动检测到配置（已在 `netlify.toml` 中设置）：
   - Build command: `npm run build`
   - Publish directory: `dist`
7. 点击 **"Deploy site"**

### 步骤 3: 等待部署完成

- 首次部署通常需要 2-5 分钟
- 可以在 Netlify 控制台查看部署日志
- 部署成功后会自动生成一个 URL，如：`https://random-name-123456.netlify.app`

### 步骤 4: 自定义域名（可选）

1. 在 Netlify 控制台，进入 **"Domain settings"**
2. 点击 **"Add custom domain"**
3. 输入你的域名（如：`www.xiaoguanxi.com`）
4. 按照提示配置 DNS 记录
5. 等待 DNS 生效（通常 24-48 小时）

### 步骤 5: 启用 HTTPS（自动）

Netlify 会自动为你的站点配置 Let's Encrypt SSL 证书，无需手动操作。

## 🔄 自动部署

配置完成后，每次推送代码到 main 分支，Netlify 会自动触发重新部署：

```bash
# 修改代码后
git add .
git commit -m "更新内容"
git push

# Netlify 会自动检测并重新部署
```

## 🛠️ 环境变量配置（如需要）

如果需要配置环境变量（如 API 密钥）：

1. 在 Netlify 控制台，进入 **"Site settings"** → **"Environment variables"**
2. 点击 **"Add a variable"**
3. 添加变量名和值
4. 重新部署站点

## 📊 性能优化建议

### 1. 图片优化

将图片压缩后再上传到 `public/assets/` 目录：

```bash
# 使用在线工具或命令行工具压缩图片
# 推荐工具：TinyPNG, ImageOptim
```

### 2. 启用缓存

Netlify 已自动配置缓存，无需额外设置。

### 3. 添加分析工具

在 `index.html` 中添加 Google Analytics：

```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-XXXXXXXXXX');
</script>
```

## 🐛 常见问题排查

### 问题 1: 部署失败

**错误信息**: `Build failed`

**解决方案**:
1. 检查 `package.json` 中的依赖是否正确
2. 本地运行 `npm run build` 测试是否能成功构建
3. 查看 Netlify 部署日志，找到具体错误信息

### 问题 2: 页面 404

**错误信息**: 刷新页面后显示 404

**解决方案**:
- 确保 `netlify.toml` 中有重定向配置（已包含）

### 问题 3: 样式丢失

**错误信息**: 页面显示但没有样式

**解决方案**:
1. 检查 `vite.config.js` 中的 `base` 配置
2. 确保 TailwindCSS 正确配置
3. 清除 Netlify 缓存后重新部署

## 📱 预览部署

Netlify 支持预览部署（Deploy Previews）：

- 每个 Pull Request 都会自动生成预览链接
- 可以在合并前预览更改效果
- 预览链接格式：`https://deploy-preview-[PR-NUMBER]--[SITE-NAME].netlify.app`

## 🔐 访问控制（可选）

如果需要限制访问：

1. 在 Netlify 控制台，进入 **"Site settings"** → **"Access control"**
2. 启用 **"Password protection"**
3. 设置密码

## 📈 监控和分析

Netlify 提供内置分析：

1. 进入 **"Analytics"** 标签
2. 查看访问量、带宽使用等数据
3. 可以升级到付费计划获取更详细的分析

## 💰 费用说明

Netlify 免费计划包括：
- ✅ 100GB 带宽/月
- ✅ 300 分钟构建时间/月
- ✅ 自动 HTTPS
- ✅ 持续部署
- ✅ 表单处理（100 次提交/月）

对于大多数个人项目和小型网站，免费计划完全够用。

## 🎉 部署完成检查清单

- [ ] 网站可以正常访问
- [ ] 所有页面和链接正常工作
- [ ] 图片正确显示
- [ ] 移动端显示正常
- [ ] HTTPS 已启用
- [ ] 自定义域名已配置（如需要）
- [ ] 分析工具已添加（如需要）
- [ ] SEO 标签已优化

## 📞 获取帮助

- Netlify 文档: https://docs.netlify.com/
- Netlify 社区: https://answers.netlify.com/
- Vite 文档: https://vitejs.dev/
- React 文档: https://react.dev/

---

祝部署顺利！🚀

