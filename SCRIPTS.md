# 📜 NPM 脚本说明

## 可用命令

### 开发命令

#### `npm run dev`
启动开发服务器

```bash
npm run dev
```

**功能**:
- 启动 Vite 开发服务器
- 默认端口：5173
- 支持热模块替换（HMR）
- 实时预览更改

**输出示例**:
```
VITE v5.0.8  ready in 500 ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

**使用场景**: 本地开发时使用

---

#### `npm run dev -- --port 3000`
指定端口启动开发服务器

```bash
npm run dev -- --port 3000
```

**功能**:
- 在指定端口启动
- 解决端口冲突问题

**使用场景**: 当 5173 端口被占用时

---

#### `npm run dev -- --host`
暴露到网络

```bash
npm run dev -- --host
```

**功能**:
- 允许局域网访问
- 可在手机上预览

**输出示例**:
```
➜  Local:   http://localhost:5173/
➜  Network: http://192.168.1.100:5173/
```

**使用场景**: 需要在手机或其他设备上测试时

---

### 构建命令

#### `npm run build`
构建生产版本

```bash
npm run build
```

**功能**:
- 编译 React 代码
- 优化和压缩资源
- 生成静态文件到 `dist` 目录
- 清除未使用的 CSS

**输出**:
```
dist/
├── index.html
├── assets/
│   ├── index-[hash].js
│   └── index-[hash].css
└── ...
```

**使用场景**: 
- 部署前构建
- 生成生产环境文件

**注意事项**:
- 构建时间：约 10-30 秒
- 产物大小：约 200-500 KB
- 自动优化和压缩

---

#### `npm run preview`
预览生产版本

```bash
npm run preview
```

**功能**:
- 预览构建后的网站
- 模拟生产环境
- 默认端口：4173

**前置条件**: 必须先运行 `npm run build`

**使用场景**: 
- 部署前测试
- 验证构建结果

---

### 其他常用命令

#### `npm install`
安装依赖

```bash
npm install
```

**功能**:
- 安装 package.json 中的所有依赖
- 生成 node_modules 目录
- 生成 package-lock.json

**使用场景**: 
- 首次克隆项目后
- 添加新依赖后

**时间**: 约 1-3 分钟

---

#### `npm install [package]`
安装新依赖

```bash
# 安装生产依赖
npm install package-name

# 安装开发依赖
npm install -D package-name
```

**示例**:
```bash
npm install axios           # 安装 axios
npm install -D eslint       # 安装 ESLint（开发依赖）
```

---

#### `npm update`
更新依赖

```bash
npm update
```

**功能**:
- 更新所有依赖到最新兼容版本
- 更新 package-lock.json

**使用场景**: 定期更新依赖

---

#### `npm outdated`
检查过期依赖

```bash
npm outdated
```

**功能**:
- 显示可更新的依赖
- 显示当前版本和最新版本

---

### 清理命令

#### 清除缓存

```bash
npm cache clean --force
```

**使用场景**: 
- 安装失败时
- 依赖出现问题时

---

#### 重新安装依赖

```bash
# 删除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

**使用场景**: 
- 依赖损坏时
- 切换 Node 版本后

---

## 🔧 自定义脚本

你可以在 `package.json` 中添加自定义脚本：

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist node_modules",
    "reinstall": "npm run clean && npm install"
  }
}
```

### 示例：添加清理脚本

```json
"clean": "rm -rf dist"
```

使用：
```bash
npm run clean
```

---

## 📊 脚本执行流程

### 开发流程

```
npm install
    ↓
npm run dev
    ↓
开发和修改代码
    ↓
实时预览更改
```

### 部署流程

```
npm install
    ↓
npm run build
    ↓
npm run preview (可选)
    ↓
部署 dist 目录
```

---

## 🐛 常见问题

### Q1: npm run dev 启动失败？

**可能原因**:
- 依赖未安装
- 端口被占用
- Node 版本不兼容

**解决方案**:
```bash
# 重新安装依赖
npm install

# 使用其他端口
npm run dev -- --port 3000

# 检查 Node 版本（需要 >= 18）
node --version
```

---

### Q2: npm run build 失败？

**可能原因**:
- 代码有错误
- 依赖缺失
- 内存不足

**解决方案**:
```bash
# 检查错误信息
npm run build

# 增加内存限制
NODE_OPTIONS=--max-old-space-size=4096 npm run build

# 清除缓存后重试
npm cache clean --force
npm install
npm run build
```

---

### Q3: npm install 很慢？

**解决方案**:
```bash
# 使用淘宝镜像
npm install --registry=https://registry.npmmirror.com

# 或永久设置
npm config set registry https://registry.npmmirror.com

# 或使用 yarn
yarn install
```

---

## 💡 最佳实践

### 1. 开发时
- 使用 `npm run dev` 启动开发服务器
- 保持开发服务器运行
- 利用热模块替换实时预览

### 2. 构建前
- 确保代码无错误
- 测试所有功能
- 检查控制台无警告

### 3. 部署前
- 运行 `npm run build`
- 运行 `npm run preview` 测试
- 检查 dist 目录大小

### 4. 定期维护
- 定期运行 `npm outdated`
- 更新过期依赖
- 清理未使用的依赖

---

## 📈 性能优化

### 构建优化

```bash
# 分析构建产物
npm run build -- --mode production

# 查看构建统计
npm run build -- --debug
```

### 开发优化

```bash
# 禁用某些检查（更快启动）
npm run dev -- --no-hmr
```

---

## 🎯 快速参考

```bash
# 常用命令
npm install              # 安装依赖
npm run dev             # 启动开发
npm run build           # 构建生产
npm run preview         # 预览生产

# 故障排查
npm cache clean --force # 清除缓存
npm outdated            # 检查更新
npm update              # 更新依赖

# 端口相关
npm run dev -- --port 3000  # 指定端口
npm run dev -- --host       # 暴露网络
```

---

需要更多帮助？查看 `README.md` 或 `QUICK_START.md`

