# 🌍 多语言功能使用指南

## 功能概述

网站现已支持中英文双语切换功能！

### 特性
- ✅ 中文 / 英文 双语支持
- ✅ 一键切换语言
- ✅ 自动保存语言偏好
- ✅ 所有页面内容完整翻译
- ✅ 浮动语言切换按钮

---

## 🎯 用户使用

### 切换语言

点击右上角的语言切换按钮：
- 显示 "EN" 时，点击切换到英文
- 显示 "中文" 时，点击切换到中文

语言偏好会自动保存到浏览器，下次访问时自动使用上次选择的语言。

---

## 🛠️ 开发者使用

### 1. 项目结构

```
src/
├── i18n/
│   ├── translations.js        # 翻译内容
│   └── LanguageContext.jsx    # 语言上下文
└── components/
    └── LanguageSwitcher.jsx    # 语言切换按钮
```

### 2. 在组件中使用多语言

#### 基本用法

```jsx
import { useLanguage } from '../i18n/LanguageContext'

function MyComponent() {
  const { t } = useLanguage()
  
  return (
    <div>
      <h1>{t('hero.title')}</h1>
      <p>{t('hero.description')}</p>
    </div>
  )
}
```

#### 获取数组

```jsx
const { tArray } = useLanguage()

const features = tArray('features.items')
features.map(item => (
  <div key={item.title}>
    <h3>{item.title}</h3>
    <p>{item.description}</p>
  </div>
))
```

#### 获取当前语言

```jsx
const { language, isZh, isEn } = useLanguage()

// language: 'zh' | 'en'
// isZh: true/false
// isEn: true/false
```

#### 手动切换语言

```jsx
const { setLanguage, toggleLanguage } = useLanguage()

// 切换到指定语言
setLanguage('en')

// 在中英文之间切换
toggleLanguage()
```

---

## 📝 添加新翻译

### 1. 编辑翻译文件

打开 `src/i18n/translations.js`，按照以下格式添加：

```javascript
export const translations = {
  zh: {
    newSection: {
      title: '新标题',
      description: '新描述'
    }
  },
  en: {
    newSection: {
      title: 'New Title',
      description: 'New Description'
    }
  }
}
```

### 2. 在组件中使用

```jsx
const { t } = useLanguage()

<h1>{t('newSection.title')}</h1>
<p>{t('newSection.description')}</p>
```

---

## 🔧 更新现有组件

### 示例：更新 Hero 组件

#### 原代码

```jsx
<h1>小习惯</h1>
<p>更轻松地养成习惯</p>
```

#### 更新后

```jsx
import { useLanguage } from '../i18n/LanguageContext'

function Hero() {
  const { t } = useLanguage()
  
  return (
    <>
      <h1>{t('hero.title')}</h1>
      <p>{t('hero.subtitle')}</p>
    </>
  )
}
```

---

## 📦 已翻译的内容

### Hero 区域
- ✅ 标题和副标题
- ✅ 描述文字
- ✅ 下载按钮
- ✅ 统计数据标签

### Features 区域
- ✅ 区域标题
- ✅ 6 个功能的标题和描述

### User Stories 区域
- ✅ 3 个用户故事
- ✅ 目标、挑战、方案、效果标签
- ✅ 统计数据标签

### Charts 区域
- ✅ 图表标题和描述
- ✅ 图表标签
- ✅ 统计亮点

### Testimonials 区域
- ✅ 6 条用户评价
- ✅ 用户信息

### Download 区域
- ✅ 下载引导文字
- ✅ 按钮文字
- ✅ 特性标签

### Footer 区域
- ✅ 所有链接
- ✅ 版权信息

---

## 🎨 语言切换按钮样式

### 位置
- 固定在右上角
- z-index: 50（最上层）

### 样式
- 白色背景
- 圆角按钮
- 阴影效果
- 悬浮动画

### 自定义样式

编辑 `src/components/LanguageSwitcher.jsx`：

```jsx
<button className="fixed top-6 right-6 ...">
  // 修改 top-6 right-6 调整位置
</button>
```

---

## 🌐 支持的语言

当前支持：
- ✅ 中文 (zh)
- ✅ English (en)

### 添加新语言

1. 在 `translations.js` 中添加新语言：

```javascript
export const translations = {
  zh: { ... },
  en: { ... },
  ja: {  // 日语
    hero: {
      title: '小さな習慣',
      // ...
    }
  }
}
```

2. 更新 `LanguageContext.jsx` 中的切换逻辑：

```javascript
const toggleLanguage = () => {
  setLanguage(prev => {
    if (prev === 'zh') return 'en'
    if (prev === 'en') return 'ja'
    return 'zh'
  })
}
```

---

## 🐛 故障排查

### 问题 1: 翻译不显示

**检查**:
1. 确保翻译 key 正确
2. 检查 translations.js 中是否有对应翻译
3. 检查是否导入了 `useLanguage`

**示例**:
```jsx
// ❌ 错误
{t('hero.titl')}  // 拼写错误

// ✅ 正确
{t('hero.title')}
```

### 问题 2: 切换语言后页面没变化

**解决方案**:
1. 检查组件是否使用了 `t()` 函数
2. 确保 LanguageProvider 包裹了 App
3. 清除浏览器缓存

### 问题 3: 语言偏好没有保存

**检查**:
- localStorage 是否可用
- 浏览器是否禁用了 localStorage

---

## 📊 翻译完成度

| 模块 | 中文 | 英文 | 完成度 |
|------|------|------|--------|
| Hero | ✅ | ✅ | 100% |
| Features | ✅ | ✅ | 100% |
| UserStories | ✅ | ✅ | 100% |
| Charts | ✅ | ✅ | 100% |
| Testimonials | ✅ | ✅ | 100% |
| Download | ✅ | ✅ | 100% |
| Footer | ✅ | ✅ | 100% |

**总完成度**: 100% ✅

---

## 🎯 最佳实践

### 1. 翻译 Key 命名

使用清晰的层级结构：

```javascript
// ✅ 好的命名
hero.title
hero.subtitle
features.items[0].title

// ❌ 不好的命名
title1
text2
desc
```

### 2. 保持翻译同步

添加中文翻译时，同时添加英文翻译：

```javascript
zh: {
  newFeature: {
    title: '新功能'
  }
},
en: {
  newFeature: {
    title: 'New Feature'  // 同时添加
  }
}
```

### 3. 使用语义化的文本

```javascript
// ✅ 好的做法
{t('download.downloadIOS')}

// ❌ 不好的做法
{language === 'zh' ? 'App Store 下载' : 'Download on App Store'}
```

---

## 📱 移动端适配

语言切换按钮已针对移动端优化：
- 响应式位置调整
- 触摸友好的按钮大小
- 清晰的视觉反馈

---

## 🚀 部署注意事项

### 1. 构建检查

```bash
npm run build
```

确保构建成功，没有翻译相关错误。

### 2. 测试

部署前测试：
- ✅ 切换语言功能正常
- ✅ 所有页面内容正确显示
- ✅ 语言偏好正确保存
- ✅ 移动端显示正常

---

## 📞 获取帮助

### 文档
- `translations.js` - 查看所有翻译
- `LanguageContext.jsx` - 查看实现逻辑

### 示例
- `Hero_i18n.jsx` - 多语言组件示例

---

## 🎉 完成！

现在你的网站已经支持中英文双语了！

**下一步**:
1. 更新所有组件使用多语言
2. 测试语言切换功能
3. 根据需要调整翻译内容

---

需要帮助？查看 `README.md` 或联系开发团队。

